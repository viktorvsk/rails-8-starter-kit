<% content_for :title, "Todo App" %>

<div class="container">
  <%= turbo_stream_from "todos" %>
  <h1>Todos</h1>

  <div id="todo_form">
    <%= render "form", todo: @todo %>
  </div>

  <ul id="todos" class="todo-list">
    <%= render @todos %>
  </ul>
</div>
